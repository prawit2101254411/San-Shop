'use client'
import React from 'react'
import SildebarForm from './SildebarForm'

type Props = {
  session : any,
}

export default async function  ({session}: Props) {
  return (
  <div className='flex border bg-white py-3'>
    san
  </div>

  )
}